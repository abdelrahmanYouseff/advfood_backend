# 🔧 إصلاح shop_id على السيرفر

## ❌ المشكلة

جميع المطاعم لديها نفس `shop_id` غير الصحيح (`821017371`):

- **Tant Bakiza**: `shop_id = "821017371"` ❌ (يجب أن يكون `"11184"`)
- **Delawa**: `shop_id = "821017371"` ❌ (يجب أن يكون `"11183"`)
- **Gather Us**: `shop_id = "821017371"` ❌ (يجب أن يكون `"11185"`)

## ✅ الحل

### الطريقة 1: استخدام Tinker (أسهل)

على السيرفر، نفّذ:

```bash
php artisan tinker
```

ثم نفّذ هذه الأوامر (بدون `>>>`):

```php
Restaurant::where('name', 'Delawa')->update(['shop_id' => '11183']);
Restaurant::where('name', 'Gather Us')->update(['shop_id' => '11185']);
Restaurant::where('name', 'Tant Bakiza')->update(['shop_id' => '11184']);
```

للتحقق:

```php
Restaurant::all(['id', 'name', 'shop_id'])->toArray();
```

ثم اكتب `exit` للخروج.

### الطريقة 2: استخدام سكريبت PHP

1. ارفع ملف `fix-shop-ids.php` على السيرفر
2. نفّذ:
   ```bash
   php fix-shop-ids.php
   ```

### الطريقة 3: تحديث مباشر في قاعدة البيانات

```sql
UPDATE restaurants SET shop_id = '11183' WHERE name = 'Delawa';
UPDATE restaurants SET shop_id = '11185' WHERE name = 'Gather Us';
UPDATE restaurants SET shop_id = '11184' WHERE name = 'Tant Bakiza';
```

## ✅ بعد التحديث

بعد تحديث `shop_id`، جرب عمل order جديد وستجد أنه يتم إرساله بنجاح لشركة الشحن!

---

**ملاحظة:** تأكد من أن هذه القيم (`11183`, `11184`, `11185`) مسجلة في نظام شركة الشحن.

